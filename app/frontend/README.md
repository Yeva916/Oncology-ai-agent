# Onco Agent Frontend

Run the frontend (requires Node.js 18+):

```bash
cd app/frontend
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` to point to your backend (default: `http://localhost:8000/api`).
